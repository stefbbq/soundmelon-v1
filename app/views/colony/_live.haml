- is_member = @colony.has_this_member? @actor
.live
  .profile-image{:title =>"#{@colony.name}"}
    = get_profile_image(@colony, @colony.profile_pic)
  .links 
    .bottom
      .name
        %h1.full-name= @colony.name
        %hr/
        = @colony.bio
      .description 
      .overlay.overlay-info
        .actions
          - unless is_member
            = link_to join_colony_path(@colony), {:id =>'unfollow-artist', :remote => true, :class => 'ajaxopen text-action'} do
              .icon-bg
              %i
              join
          - else
            = link_to join_colony_path(@colony), {:id =>'unfollow-artist', :remote => true, :class => 'ajaxopen text-action'} do
              .icon-bg
              %i
              unjoin
          = link_to colony_members_path(@colony), {:id =>'unfollow-artist', :remote => true, :class => 'ajaxopen text-action'} do
            .icon-bg
            %i
            members
        .heading