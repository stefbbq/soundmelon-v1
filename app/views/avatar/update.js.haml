$(':submit').removeAttr('disabled');
jQuery.facebox("Successfully cropped");
setTimeout('$(document).trigger(\"close.facebox\")',1000);
!= "$('.live .profile-image').html('#{escape_javascript(get_avatar_large(@user, true))}');"

