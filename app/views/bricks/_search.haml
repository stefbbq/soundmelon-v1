.search
  =form_tag( {:controller =>'search', :action => 'index'}, :method => "get") do
    %input.auto_search_complete{:value => "search", :name => "q", :type => "text", :class => 'search-input'}
:javascript
  $('.search-input').focus(function() {
    $(this).val("");
    });