Hello #{@recipeint.get_name}
- if @action_item.blank?
  #{@actor.get_name} started to follow you on SoundMelon.
- else
  #{@actor.get_name} started to follow your artist "#{@action_item.get_name}" on SoundMelon.