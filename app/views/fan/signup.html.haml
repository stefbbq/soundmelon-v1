- if @user.email        
  = form_for :user, :url=>fan_registration_path do |f|
    .blankbox
      %span.green{:style=>'margin-left:320px;font-size:16px;color:green'}
        Sign-up, it's free.
      .content-box
        .registrationform
          = render '/fan/registration-fields'

  :javascript
    $(document).ready(function() {
      $("#genre").tokenInput(
        [
        #{genre_atuofill}
        ],
        {
          theme: "facebook"
        }
      );
    });
- else  
  .beta-note
    %div
      %h2 We are currently in closed beta. 
      %p Please #{link_to "request an invitation", new_invitation_path, :remote =>:true} and we will notify you when we are ready to accept more users.