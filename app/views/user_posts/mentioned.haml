= render :partial=>"fan/live"
.primary-container
  .primary
    .left
      = render "user_posts/form"
      = render "fan/social_header", :current =>'mention'
      = render "bricks/fan_posts", :current =>'mention'
    .right
      = render "fan/right"