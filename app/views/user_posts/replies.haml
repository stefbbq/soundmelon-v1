= render :partial=>"/fan/live"
.primary-container
  .primary
    .left
      .top
        = render "/user_posts/status_fan"
        = render "/fan/social_header", :current =>'reply'
      = render "/bricks/fan_posts", :current =>'reply'
    .right
      = render "/fan/right"