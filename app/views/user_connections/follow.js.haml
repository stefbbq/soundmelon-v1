$('#facebox .content').html('Followed Successfully');
setTimeout('$(document).trigger(\"close.facebox\")',1000);
- unfollow_content = capture do
  = link_to 'unfollow', unfollow_user_path(@user_to_be_followed, :others => 1), :remote => true, :class => 'ajaxopen'
$('.actions #follow_link').html('#{escape_javascript unfollow_content}');
$('#follow-unfollow .selection-box').html('#{escape_javascript unfollow_content}');