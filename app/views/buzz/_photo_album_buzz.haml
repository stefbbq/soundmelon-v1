.buzz_header
  - if @band_photo_album
    = "Buzz : #{@band_photo_album.name}"
  - else
    = "Buzz : #{@band_photo.caption}"
%hr/
= render 'buzzlisting', :buzzes_by_date =>@buzzes_by_dates
.buzz_form
  = render 'photo_album_buzz_form'
