#{@artist.get_name} is using the
= link_to "SoundMelon Beta", root_path, :target=>'_blank', :title =>"SoundMelon"
= link_to "request an invite", new_invitation_path(1), :remote =>:true, :id =>'logout', :class =>'request-invite'

    