#footer
  #page-bottom-fade
  .content
    %div
      = link_to page_path('about'), :remote =>:true, :class =>'force-login' do
        = image_tag 'base-footer-link-about.png'
      = link_to page_path('contact'), :remote =>:true, :class =>'force-login' do
        = image_tag 'base-footer-link-contact.png'
      = link_to page_path('help') ,:remote =>:true, :class =>'force-login' do
        = image_tag 'base-footer-link-help.png'
      = link_to page_path('terms') ,:remote =>:true, :class =>'force-login' do
        = image_tag 'base-footer-link-terms.png'