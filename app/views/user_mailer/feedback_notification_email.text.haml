There is new feedback
\===============================================

#{@content}

\================================================
- url = @user.is_fan? ? fan_profile_url(@user) : show_band_url(@user.name)

Sent By : #{url}

