alert('Band Details Successfully Updated.');
$('.top-bar .session .summary').html('#{escape_javascript(render '/bricks/session_header')}');
$('.area .details h3 span#artistname').html('#{@band.name}');
$('.live').replaceWith('#{escape_javascript(render 'live')}');
$('.primary .right').html('#{escape_javascript(render 'right', :current =>'manage_profile')}');
setTimeout('$(document).trigger(\"close.facebox\")',10);
