- content = capture do
  - if @venues.blank?
    You have no venue profiles associated with your account, would you like to #{link_to 'create one?', new_venue_path, :class => 'ajaxopen backable', :remote => true}
  - else
    - @venues.each do |venue|
      = render '/venue/venue_item', :venue =>venue
    .message
      New venue profile has been created.
    %hr
    #{link_to 'create another one?', new_venue_path, :class => 'ajaxopen backable', :remote => true} You can add another venue profile to associate with your account!
updateLeftSection('#{escape_javascript(content)}');
$('.selection-box').removeClass('active').removeClass('button-fill-fan');
$('#access-venue').addClass('active').addClass('button-fill-fan');