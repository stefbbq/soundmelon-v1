#venue_state_code_wrapper
  - parent_region ||= params[:parent_region]
  - country = Carmen::Country.coded(parent_region)
  - if country.nil?    
  - elsif country.subregions?
    = subregion_select(:venue, :state, parent_region)
  - else
    = text_field(:venue, :state)
