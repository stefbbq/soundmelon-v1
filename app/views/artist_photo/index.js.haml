- artist_albums = capture do
  #photo_album_box
    - if @artist_album_list.empty?
      - if @has_link_access
        .add-action-space
          share photos of you or your gigs
          = link_to "add album", new_artist_album_path(@artist), :remote=>:true, :class=>"ajaxopen add-new"
    - else
      - @artist_album_list.each do |artist_album|
        = render '/artist_photo/album', :artist_album =>artist_album, :show_all =>@show_all || false
updateLeftSection('#{escape_javascript artist_albums}');
updateRightSection('#{escape_javascript(render '/artist_public/right', :current => 'photo')}');
closeFacebox(1);
scrollToTop();