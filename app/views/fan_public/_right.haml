.items
  = link_to 'latest posts', fan_profile_path(@user), :id=>'latest-posts', :remote=>:true, :class=>'ajaxopen selection-box'
  #artistbox.box-wrapper
    .selection-box
      following
      %span.artist-count
        = pluralize(@following_artist_count, 'artist')
      %br/
      = link_to 'see all', fan_following_artists_path(@user), :remote=>:true, :class=>'small-font ajaxopen'
    .band-box-content
      %div
        - @following_artists.each do |band|
          .artist-avatar
            = link_to get_band_logo_small(band), fan_following_artists_path(:band_name => band.name)
      .clear
  .clear
  #followingbox.box-wrapper
    .selection-box
      following
      %span.artist-count
        = pluralize(@following_count,'fan')
      %br/
      = link_to 'see all', fan_following_path(@user), :remote=>:true, :class=>'small-font ajaxopen'
    .band-box-content
      %div
        - @following_users.each do |fan_following|
          .artist-avatar
            = link_to get_avatar_small(fan_following), fan_profile_path(fan_following)
      .clear
  .clear
  #followerbox.box-wrapper
    .selection-box
      %span.artist-count
        = pluralize(@followers_count,'follower')
      %br/
      = link_to 'see all', fan_followers_path(@user), :remote=>:true, :class=>'small-font ajaxopen'
    .band-box-content
      %div
        - @followers_users.each do |fan_follower|
          .artist-avatar
            = link_to get_avatar_small(fan_follower), fan_profile_path(fan_follower)
      .clear
  .clear
