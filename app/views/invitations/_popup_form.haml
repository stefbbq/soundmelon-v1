- @invitation ||= Invitation.new
= form_for @invitation, :url =>{:controller=>'invitations', :action =>'create_invitation'}, :remote => true, :id => 'homepage_invitation_form' do |f|    
  email
  = f.text_field :recipient_email, :class => 'overlay-text-field', :style => "width: 300px; margin-bottom: 10px;"
  = f.submit 'submit', :class => "generic-buttonl"
- if !@invitation.errors.blank?
  .error
    %ul
    - @invitation.errors.full_messages.each do |error_msg|
      %li
        = error_msg